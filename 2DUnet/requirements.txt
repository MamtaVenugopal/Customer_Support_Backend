# DCASE bearing U-Net ASD (local + Google Colab)
# Do NOT add a plain "torch" line here if you rely on Google Colab's CUDA build.
# Install:  pip install -r requirements.txt
# (Notebook section 0 runs this automatically.)

librosa>=0.10.0
soundfile>=0.12.0
numpy>=1.26.0
pandas>=2.0.0
matplotlib>=3.8.0
scikit-learn>=1.3.0
tqdm>=4.66.0
torchinfo>=1.8.0
nbformat>=5.10.0
